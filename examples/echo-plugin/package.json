{
  "name": "@tegojs/echo-plugin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm exec --offline -- tsc -p tsconfig.json",
    "test:unit": "npm run build && node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false && tsc --ignoreConfig --noEmit --pretty false --target ES2024 --module NodeNext --moduleResolution NodeNext --types node --strict --noUncheckedIndexedAccess --exactOptionalPropertyTypes --verbatimModuleSyntax --skipLibCheck test/component.test.ts"
  },
  "dependencies": {
    "@tegojs/plugin-sdk": "0.0.0"
  }
}
